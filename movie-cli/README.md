# @gorenku/movie-cli

Movie Studio command surface.

This package is intentionally separate from the existing `@gorenku/cli` package. It will expose commands for Movie Studio projects, tasks, workflows, queue operations, approvals, and agent-facing interactions.

