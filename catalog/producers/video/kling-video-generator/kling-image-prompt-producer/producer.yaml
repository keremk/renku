meta:
  name: Kling Image Prompt Producer
  description: Converts one semantic scene intent into a Kling image-to-video prompt.
  id: KlingImagePromptProducer
  kind: producer
  version: 0.1.0
  author: Renku
  license: MIT
  promptFile: ./prompts.toml
  outputSchema: ./output-schema.json

inputs:
  - name: SceneIntent
    description: Core visible scene, subject, and action.
    type: string
  - name: CameraIntent
    description: Camera movement and framing intent.
    type: string
  - name: AudioIntent
    description: Sound-design or dialogue intent when native audio is requested.
    type: string
  - name: EnvironmentAndStyle
    description: Environment, lighting, and visual texture.
    type: string
  - name: ReferenceInstructions
    description: Optional instructions for supplied Kling elements.
    type: string
    required: false
  - name: ShotBreakdown
    description: Optional shot-by-shot guidance to fold into one prompt.
    type: string
    required: false
  - name: UseNativeAudio
    description: Whether native clip audio is wanted.
    type: boolean

outputs:
  - name: Prompt
    description: Final Kling image-to-video prompt for one clip.
    type: string
