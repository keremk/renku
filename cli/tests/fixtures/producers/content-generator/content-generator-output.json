{
  "name": "Script",
  "strict": true,
  "schema": {
    "type": "object",
    "properties": {
      "Title": {
        "type": "string",
        "description": "The title of the content."
      },
      "Clips": {
        "type": "array",
        "description": "Array of content clips.",
        "items": {
          "type": "object",
          "properties": {
            "ImagePrompt": {
              "type": "string",
              "description": "Prompt for generating the clip image."
            },
            "VideoPrompt": {
              "type": "string",
              "description": "Prompt for generating the clip video."
            },
            "VideoDescription": {
              "type": "string",
              "description": "Description for the clip video."
            }
          },
          "required": ["ImagePrompt", "VideoPrompt", "VideoDescription"],
          "additionalProperties": false
        }
      }
    },
    "required": ["Title", "Clips"],
    "additionalProperties": false
  }
}
