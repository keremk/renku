meta:
  name: Generate Long Form Video Prompt
  description: Generate prompts for long form video content from clips with begin and end images
  id: FlowVideoPromptProducer
  kind: producer
  version: 0.1.0
  author: Renku
  license: MIT
  promptFile: ./flow-video.toml
  outputSchema: ./flow-video-output.json

inputs:
  - name: InquiryPrompt
    description: The topic describing the desired movie script.
    type: string
  - name: Duration
    description: Desired narration duration in seconds.
    type: int
  - name: NumOfClips
    description: Number of narration clips to produce.
    type: int
  - name: Style
    description: Desired visual style to feed into prompt.
    type: string
  - name: Audience
    description: Intended audience for tailoring details. Model default varies.
    type: string

outputs:
  - name: MovieTitle
    description: The generated title for the movie.
    type: string
  - name: NarrationScript
    description: Array of narration paragraphs per clip.
    type: array
    itemType: string
    countInput: NumOfClips
  - name: VideoPrompts
    description: Array of video prompts per clip.
    type: array
    itemType: string
    countInput: NumOfClips
  - name: ImagePrompts
    description: Array of image prompts per clip.
    type: array
    itemType: string
    countInput: NumOfClips
    countInputOffset: 1
