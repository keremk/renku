meta:
  name: Flow Video Generation
  description: Generate video from segments with first and last frame continuity.
  id: FlowVideo
  version: 0.1.0
  author: Renku
  license: MIT

inputs:
  - name: Duration
    description: Total movie duration in seconds.
    type: int
    required: true
  - name: NumOfSegments
    description: Number of narration/video segments.
    type: int
    required: true
  - name: InquiryPrompt
    description: The prompt describing the movie script to be generated.
    type: string
    required: true
  - name: Audience
    description: Target audience for tone and vocabulary.
    type: string
    required: false
  - name: Language
    description: Narration language.
    type: string
    required: false
  - name: Style
    description: Desired movie style to feed into prompt.
    type: string
    required: true
  - name: Resolution
    description: Video and image resolution as width/height pixel dimensions.
    type: resolution
    required: true
  - name: VoiceId
    description: The voice identifier to feed into audio generation.
    type: string
    required: true
  - name: Emotion
    description: Optional emotion hint for narration.
    type: string
    required: false

outputs:
  - name: SegmentVideo
    description: Generated video for each narration segment.
    type: array
    itemType: video
    countInput: NumOfSegments
  - name: SegmentAudio
    description: The narrated audio segments for the movie.
    type: array
    itemType: audio
  - name: Timeline
    description: The composed timeline JSON manifest.
    type: json

loops:
  - name: segment
    description: Iterates over narration segments.
    countInput: NumOfSegments
  - name: image
    description: Iterates over images for each segment, one more than the number of segments
    countInput: NumOfSegments
    countInputOffset: 1

imports:
  - name: FlowVideoPromptProducer
    path: ../../_shared/flow-video/flow-video.yaml
  - name: ImageProducer
    producer: image/text-to-image
    loop: segment
  - name: ImageToVideoProducer
    producer: video/image-to-video
    loop: segment
  - name: AudioProducer
    producer: audio/text-to-speech
    loop: segment
  - name: TimelineComposer
    producer: composition/timeline-composer

connections:
  - from: InquiryPrompt
    to: FlowVideoPromptProducer.InquiryPrompt
  - from: Audience
    to: FlowVideoPromptProducer.Audience
  - from: Duration
    to: FlowVideoPromptProducer.Duration
  - from: Style
    to: FlowVideoPromptProducer.Style
  - from: NumOfSegments
    to: FlowVideoPromptProducer.NumOfSegments

  - from: FlowVideoPromptProducer.ImagePrompts[image]
    to: ImageProducer[image].Prompt
  - from: Resolution
    to: ImageProducer[image].Resolution

  - from: FlowVideoPromptProducer.VideoPrompts[segment]
    to: ImageToVideoProducer[segment].Prompt
  - from: ImageProducer[image].GeneratedImage
    to: ImageToVideoProducer[segment].StartImage
  - from: ImageProducer[image+1].GeneratedImage
    to: ImageToVideoProducer[segment].EndImage
  - from: Resolution
    to: ImageToVideoProducer[segment].Resolution
  - from: SegmentDuration
    to: ImageToVideoProducer[segment].Duration
  - from: ImageToVideoProducer[segment].GeneratedVideo
    to: SegmentVideo[segment]
  - from: ImageToVideoProducer[segment].GeneratedVideo
    to: TimelineComposer.VideoSegments

  - from: FlowVideoPromptProducer.NarrationScript[segment]
    to: AudioProducer[segment].Text
  - from: SegmentDuration
    to: AudioProducer[segment].Duration
  - from: VoiceId
    to: AudioProducer[segment].VoiceId
  - from: Emotion
    to: AudioProducer[segment].Emotion
  - from: AudioProducer[segment].GeneratedAudio
    to: SegmentAudio[segment]
  - from: AudioProducer[segment].GeneratedAudio
    to: TimelineComposer.AudioSegments

  - from: Duration
    to: TimelineComposer.Duration
  - from: TimelineComposer.Timeline
    to: Timeline
