# @gorenku/movie-core

Movie Studio domain package.

This package is the new home for Movie Studio-specific project, workflow, queue, task, take, and projection primitives. It intentionally starts separate from the existing `@gorenku/core` blueprint/build system so Movie Studio can grow around its own domain model.

